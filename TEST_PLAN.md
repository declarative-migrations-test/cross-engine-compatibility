# Test plan

- Verify portable plan equivalence and preflight classification across PostgreSQL and CockroachDB across the supported happy-path states and canonical fixtures.
- Verify portable plan equivalence and preflight classification across PostgreSQL and CockroachDB under retries, interruption, concurrency, offline operation, or partial failure.
- Verify portable plan equivalence and preflight classification across PostgreSQL and CockroachDB preserves authorization, idempotency, integrity, observability, and actionable failure classification.

## Classification

- product regression
- blocked dependency
- harness regression
